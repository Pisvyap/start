delo factorial(n: chislo) -> chislo {
        esli (n = 0) {
            n <- 1;
        } inache {
            n <- n * (factorial <- [n - 1]);
        }
        n -> otday;
}
[20] -> factorial -> glaza;
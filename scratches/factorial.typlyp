delo factorial(n: chislo): chislo {
        pust k: chislo = n;
        esli (n == 0) {
            k = 1;
        } inache {
            k = k * factorial(k - 1);
        }

        vernut k;

}

pust result: chislo = factorial(12);
result -> glaza;
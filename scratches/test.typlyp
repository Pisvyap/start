delo factorial(n: chislo): chislo {
        esli (n == 0) {
            n = 1;
        } inache {
            n = n * factorial(n - 1);
        }
        vernut n;
}
pust result: chislo = factorial(20);
result -> glaza;